{
    "builds": [{
        "src": "learning_platform/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb", "runtime": "python3.9" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "learning_platform/wsgi.py"
        }
    ]
}
